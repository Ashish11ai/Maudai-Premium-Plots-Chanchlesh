# Project Rules

- The One CLI (`one`) is installed on this machine to interact with 400+ third-party platforms (Gmail, Slack, Shopify, HubSpot, Stripe, GitHub, etc.) through their APIs. Whenever you need to interact with any third-party platform or external service, use the `one` CLI / integrations.
